Im creating an auto installable arch rice based on hyprland.
I want the rice to be terminal first, so everything that can be done on the terminal without much complication should be done there, thats why it has packages like NeoMutt for email, NeoVim as code/text editor, or Yazi as file explorer.
Check packages.txt to see what we are using on our rice and check the install script to see other things that may be installed that are not shown in packages.txt.
Then tell me the structure for dotfiles, which will be .config files, .local files and files located under /home/$USER/ that are configs too.
Give me insights on what should be configured in the folders and what should be dinamically added via scripts for our multi theme experience.
To give further insight in our rice, Im seeking for something:
 - minimal and simple, but with good interactivity and usefull widgets, tool bar, etc.
 - 4 workspaces, unable creation of more (no one needs more)
 - Theme and Wallpaper switch with rofi menu, where depending on the selected theme, the poll of wallpapers should change (meaning wallpaper matches theme and not the other way arround). Themes should affect all apps possible, meaning we need GTK theming, hyprland theming, terminal theming.
 - User friendly, since this is intended to be a repo so that new comers to arch and hyprland can avoid ricing by them selves everything.

I also was seeking into having a better install script, with less failure possible points and that will be able to detect the main GPU and only install drivers for those.

The theming should work like it follows:
 - A themes folder should be created inside .local or .config, I dont mind which one, there we would have folders with theme names, and inside the folders files like colors.conf, or colors.css, etc, with all the color pallets and a folder named wallpapers.
 - When selecting a theme with a script that we will create, the files iside /themes/<theme>/ will be copied to their respective paths in .config, home and .local for theming the apps, the configs of this apps should reference that file name always, this way, theres no touching the configuration, but changing the colors source file content by another one. The script should also copy the wallpapers into Images/Wallpapers in the home directory.
 - The wallpaper selector script will only look for wallpapers in that directory, and thus, will only find the ones of the current theme.
 - Then we should configure rofi menus for both the theme switch script and the wallpaper switch script, so that users may change theme and wallpaper without having to use the terminal.
 - In addition, I would like to add animations to theme and wallpaper switches, thats why we use swww as the wallpaper engine.
