# =============================================================================
#  Device-Specific Configuration
#  External device setup
# =============================================================================

# Example: External mouse with specific settings
device:logitech-usb-receiver {
    sensitivity = 0.5
}

# Example: Generic USB keyboard
device:usb-keyboard {
    kb_layout = us
}

# Note: Run 'hyprctl devices' to find your device names
